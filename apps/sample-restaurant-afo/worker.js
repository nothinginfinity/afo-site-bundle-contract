export default { async fetch(request) { const url = new URL(request.url); if (url.pathname === '/') return new Response('<h1>Sample Restaurant</h1>', { headers: { 'content-type': 'text/html; charset=utf-8' } }); if (url.pathname === '/health') return new Response(JSON.stringify({ ok: true, worker: 'sample-restaurant-afo' }), { headers: { 'content-type': 'application/json; charset=utf-8' } }); if (url.pathname === '/llms.txt') return new Response('# Sample Restaurant\n', { headers: { 'content-type': 'text/plain; charset=utf-8' } }); if (url.pathname === '/robots.txt') return new Response('User-agent: *\nAllow: /\n', { headers: { 'content-type': 'text/plain; charset=utf-8' } }); if (url.pathname === '/sitemap.xml') return new Response('<?xml version="1.0"?><urlset></urlset>', { headers: { 'content-type': 'application/xml; charset=utf-8' } }); if (url.pathname === '/schema.json') return new Response(JSON.stringify({ '@context': 'https://schema.org', '@type': 'Restaurant', name: 'Sample Restaurant' }), { headers: { 'content-type': 'application/json; charset=utf-8' } }); return new Response('Not found', { status: 404 }); } };