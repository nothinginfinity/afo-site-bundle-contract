name = "example-business-afo"
main = "src/index.ts"
compatibility_date = "2026-06-01"

# Preview-safe reference config.
# GitHub is the source of truth.
# Cloudflare is the runtime target only.
# Do not add production routes, account_id, custom domains, or secrets here.
# Deploy only through AFO Mobile Terminal MCP after explicit confirmation.

[build]
command = "npm run build"
