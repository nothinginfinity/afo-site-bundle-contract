{
  "name": "example-business-afo",
  "version": "1.0.0",
  "type": "module",
  "description": "Preview-safe Cloudflare Worker reference implementation for AFO Site Bundle Manifest v1.",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "dev": "wrangler dev",
    "preview": "wrangler dev --remote",
    "deploy": "echo 'Do not deploy manually. Use AFO Mobile Terminal MCP.' && exit 1"
  },
  "dependencies": {},
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250601.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.0.0"
  }
}
