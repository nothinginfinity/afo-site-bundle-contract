{
  "name": "afo-site-bundle-contract",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Shared AFO Site Bundle Manifest v1 contract and preview-safe example bundle.",
  "scripts": {
    "validate": "node scripts/validate-bundle.mjs",
    "mobile-terminal:dry-run": "node scripts/mobile-terminal-dry-run.mjs",
    "mobile-terminal:write-dry-run-receipt": "node scripts/mobile-terminal-dry-run.mjs --write-receipt",
    "check": "npm run validate && npm run mobile-terminal:dry-run && npm --prefix examples/example-business/worker run build"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
